# 큐브 타임어택 — 작업 규칙

## GitHub 동기화 (필수)

이 저장소는 여러 컴퓨터에서 이어서 작업한다. 작업 내용은 GitHub(`origin/main`)에 있어야만 다른 컴퓨터에서 이어받을 수 있다.

- **작업 시작 전**: `git pull`로 최신 상태를 받는다.
- **작업이 한 단위 끝날 때마다**: 변경 사항을 커밋하고 `git push`까지 한다. 작업 결과를 보고하기 전에 push를 마친다.
- 커밋 메시지는 한국어로, 무엇을 바꿨는지 한 줄로 쓴다.
- push가 실패하면 (충돌, 인증 만료 등) 그대로 두지 말고 사용자에게 원인과 해결 방법을 알린다.
- 커밋 작성자 이메일은 GitHub noreply 주소를 쓴다 (실제 이메일 노출 방지).

## 프로젝트 구조

- `index.html` — 게임 전체 (HTML·CSS·JS 한 파일, three.js r128을 CDN으로 불러옴). 빌드 과정 없음.
- `README.md` — 기능과 조작법 설명. 기능을 바꾸면 README도 함께 고친다.
